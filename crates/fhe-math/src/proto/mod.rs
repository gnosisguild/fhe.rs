//! Protobuf for the `fhe-math` crate.

// The generated wire format below is re-exported for threshold schemes
// building explicit transport boundaries; it carries no hand-written docs.
#[allow(missing_docs)]
/// Protobuf for polynomials.
pub mod rq;
