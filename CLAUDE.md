# fhe.rs

`AGENTS.md` is the canonical repository guide for AI coding agents. Read it before starting work.

Before editing, use its **Touch → update** table to identify and read the relevant files under [`.rules/`](.rules). The `.rules/` files are part of the repository's canonical development guidance.

## What applies to this session

- `AGENTS.md` and `.rules/` — shared, harness-neutral. Follow them.
- `CLAUDE.md` (this file) — adapter only.
- `.opencode/` (agents, skills, and config) — OpenCode-specific. Do not assume they apply to this session; they are inert to Claude Code.
